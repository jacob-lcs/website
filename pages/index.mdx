---
title: 基于 Rust 的 web 平台
---

import Callout from 'nextra-theme-docs/callout'

<style jsx global>{`
body {
  background-image: radial-gradient(#ddd 1px, transparent 0),
    radial-gradient(#ddd 1px, transparent 0);
  background-position: 0 0, 25px 25px;
  background-size: 50px 50px;
}
.dark body {
  background-image: radial-gradient(#333 1px, transparent 0),
    radial-gradient(#333 1px, transparent 0);
}
`}</style>

{
// wrapped with {} to mark it as javascript so mdx will not put it under a p tag
}
{<h1 className="text-center font-extrabold md:text-5xl mt-8">SWC</h1>}

<div className="mx-auto max-w-full w-[880px] text-center px-4 my-10">
  <p className="text-lg mb-2 text-gray-600 md:!text-2xl">
    基于 Rust 的 web 平台
  </p>
</div>

SWC 是一个基于 Rust 的可拓展性的平台，用于下一代快速开发工具。它被 Next.js、Parcel 和 Deno 等工具使用，Vercel、字节跳动、腾讯、Shopify 等公司也在使用 SWC。

SWC 可以用于编译和打包。对于编译，SWC 将处理使用现代 JavaScript 特性的 JavaScript / TypeScript 文件并将其输出为支持所有主流浏览器的代码。

<Callout emoji="🏎">
  SWC 在单线程情况下<strong>比 Babel 块 20 倍</strong>，四核下要快<strong> 70 倍</strong>。
</Callout>

<div className="mt-16 mb-20 text-center">
  [快速开始](/docs/getting-started) · [演练场](/playground) · [博客](/blog/perf-swc-vs-babel) · [Rust 文档](https://rustdoc.swc.rs/swc/) · [GitHub 仓库](https://github.com/swc-project/swc) · [捐赠](https://opencollective.com/swc)
</div>

## Overview

```bash
# 下载预构建的二进制文件
npm i -D @swc/cli @swc/core

# 转换 JavaScript 文件并输出到 stdout
npx -p @swc/cli swc ./file.js
```

SWC 下载后可以作为一个预构建二进制文件使用，也可以从源码构建。现在提供了以下二进制文件：

- Mac (Apple Silicon)
- Mac (x64)
- Linux (x86_64)
- Linux (aarch64)
- Linux (armv7)
- Alpine Linux (也要安装 `@swc/core-linux-musl`)
- Android (aarch64)
- Windows (win32-x64)
- Windows (ia32)

## 特性

SWC 被设计为可拓展的。现在支持：

- 编译
- 打包（`swcpack`）
- 压缩
- 使用 WebAssembly 转换
- 在 webpack 中使用（`swc-loader`）
- 提升 Jest 性能（`@swc/jest`）
- 自定义插件

[查看更多](/docs/getting-started).

## 社区

<p className="flex h-6">
  <img alt="stars" src="https://badgen.net/github/stars/swc-project/swc" className="inline-block mr-2"/>
  <img alt="downloads" src="https://badgen.net/npm/dw/@swc/core" className="inline-block mr-2"/>
</p>

SWC 作者是 [kdy1dev](https://twitter.com/kdy1dev)。
在 Twitter 上关注 [@kdy1dev](https://twitter.com/kdy1dev) 以获取之后的项目更新。

欢迎加入 [discussions on GitHub](https://github.com/swc-project/swc/discussions)！
